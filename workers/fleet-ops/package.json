{
  "name": "grudge-fleet-ops",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker control plane for Grudge fleet probes and AI job plans",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit -p tsconfig.json || true"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250317.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
